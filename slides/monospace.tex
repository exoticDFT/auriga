\begin{frame}[fragile]{A slide with some code}

	\begin{columns}
		\begin{column}{0.5\linewidth}
			\footnotesize
      % For more languange options (https://pygments.org/languages/)
      \begin{minted}{python}
        /* some code */
        def foo(x):
          return x**0.5 + 2*x
      \end{minted}
    \end{column}
    \begin{column}{0.5\linewidth}
      {\color{cardinalred} Some explanatory text, in red, with some \texttt{monospace} text.}
      There might be some math, too:

      $$\sqrt{x} + 2x$$
    \end{column}
  \end{columns}

\end{frame}
